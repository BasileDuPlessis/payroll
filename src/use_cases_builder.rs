use crate::use_cases::AddEmployee;

struct AddEmployeeBuilder {
    add_employee: AddEmployee,
    db: PayrollDB,
}

impl AddEmployeeBuilder {
    fn new() -> AddEmployeeBuilder {

    }

    fn pay_type() -> AddEmployeeBuilder {

    }

    fn execute() {

    }
}